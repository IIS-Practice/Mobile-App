import React from "react";
import { View, Text } from "react-native";

const Scrum = ({ navigation }) => (
  <View>
    <Text style={{ color: "black" }}>Scrum Screen</Text>
  </View>
);

export default Scrum;
