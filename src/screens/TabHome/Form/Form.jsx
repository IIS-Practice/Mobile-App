import React from "react";
import { View, Text } from "react-native";

const Form = ({ navigation }) => (
  <View>
    <Text style={{ color: "black" }}>Form Screen</Text>
  </View>
);

export default Form;
