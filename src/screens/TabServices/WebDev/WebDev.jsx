import React from "react";
import { View, Text } from "react-native";

const WebDev = ({ navigation }) => (
  <View>
    <Text style={{ color: "black" }}>WebDev Screen</Text>
  </View>
);

export default WebDev;
