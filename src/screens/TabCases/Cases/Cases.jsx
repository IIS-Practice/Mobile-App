import React from "react";
import { View, Text } from "react-native";

const Cases = ({ navigation }) => (
  <View>
    <Text style={{ color: "black" }}>Cases Screen</Text>
  </View>
);

export default Cases;
